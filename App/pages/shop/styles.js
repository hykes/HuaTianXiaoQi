'use strict';

import {
  StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1
  },
  page: {
       flex: 1,
       height: 180,
       resizeMode: 'stretch'
   }
});

export default styles;
